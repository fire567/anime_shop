import { css } from "@emotion/css";

export const adminLoginContainerWrapper = css({
    width: "100vw",
    height: "100vh",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
});
