import { css } from "@emotion/css";

export const mainPageContent = css({
    width: "1268px",
    marginTop: "60px",
    display: "flex",
});

export const mainPageWrapper = css({
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "#FCF8F2",
    marginTop: "60px",
});
