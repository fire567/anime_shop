import { css } from "@emotion/css";

export const emptyCartWrapper = css({
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    fontSize: "30px",
    height: "60px",
    color: "#3D3D3D",
});
