import { css } from "@emotion/css";

export const header_wrapper = css({
    backgroundColor: "#F7C40A",
    height: "60px",
    display: "flex",
    justifyContent: "center",
});

export const header_content = css({
    width: "1268px",
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
});
