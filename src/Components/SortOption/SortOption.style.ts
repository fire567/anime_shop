import { css } from "@emotion/css";

export const sortOptionWrapper = css({
    marginLeft: "20px",
    display: "flex",
    cursor: "pointer",
    fontSize: "16px",
    color: "#3D3D3D",
});

export const triangleWrapper = css({
    marginLeft: "10px",
});
