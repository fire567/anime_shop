import { css } from "@emotion/css";

export const priceFilterWrapper = css({
    display: "flex",
    justifyContent: "space-between",
    paddingRight: "50px",
});

export const priceFilter = css({
    width: "70px",
});
