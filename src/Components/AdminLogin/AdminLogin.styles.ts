import { css } from "@emotion/css";

export const adminLoginWrapper = css({
    width: "600px",
});

export const adminHeaderWrapper = css({
    height: "60px",
    fontSize: "30px",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#F7C40A",
    color: "#FCF8F2",
});

export const formWrapper = css({
    height: "200px",
    display: "flex",
    flexDirection: "column",
    justifyContent: "space-around",
    alignItems: "center",
});

export const buttonWrapper = css({
    height: "40px",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
});
