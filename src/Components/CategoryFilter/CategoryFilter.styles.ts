import { css } from "@emotion/css";
export const categoryFilterWrapper = css({
    width: "248px",
});

export const title = css({
    width: "100%",
    fontSize: "21px",
    color: "#3D3D3D",
    marginBottom: "30px",
    display: "flex",
    justifyContent: "center",
});

export const buttonWrapper = css({
    width: "100%",
    display: "flex",
    justifyContent: "center",
    marginTop: "30px",
});

export const button = css({
    background: "#F7C40A",
    "& :hover": {
        background: "#F7C40A",
    },
});
