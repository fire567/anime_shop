import { css } from "@emotion/css";

export const currentCategoryWrapper = css({
    padding: "0px",
    marginTop: "16px",
    display: "flex",
    flexDirection: "column" as "column",
});

export const category_title = css({
    fontSize: "17px",
    color: "#3D3D3D",
});
